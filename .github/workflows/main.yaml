name: Test

on:
  pull_request:
    branches:
      - main

  workflow_dispatch:

jobs:
  build-hadchi:
    runs-on: ubuntu-latest

    steps:
      - name: checkout
        uses: actions/checkout@v3
      
      - name: test docker
        run: docker build -t test .